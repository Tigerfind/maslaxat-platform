# Dependencies
node_modules/
*/node_modules/
# Lock-файлы по умолчанию не коммитим, и в загрузку Railway они тоже не идут.
# Исключение — бэкенд: его Dockerfile собирает через `npm ci`, которому
# package-lock.json обязателен. Фронтенду lock отдавать нельзя: с ним nixpacks
# переключается на `npm ci`, а npm 10 на билдере Railway отвергает наш файл
# (Missing: yaml) — там сборка должна идти через `npm install`.
package-lock.json
!backend/api/package-lock.json
yarn.lock
.pnp
.pnp.js

# Testing
coverage/
.nyc_output/

# Production
build/
dist/
*/build/
*/dist/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
*.env

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# OS
.DS_Store
Thumbs.db

# IDEs
.idea/
.vscode/
*.swp
*.swo
*.swn
*.bak
*~

# Docker
docker-compose.override.yml

# Database
*.sql
*.sqlite
*.db

# Uploads and temp files
uploads/
tmp/
temp/

# Redis dump
dump.rdb

# Python
__pycache__/
*.py[cod]
*$py.class
.Python
*.so

# Java/Kotlin
*.class
*.jar
*.war

# Mobile
.expo/
.expo-shared/

# Certificates and keys
*.pem
*.key
*.cert
*.crt

# Secrets
secrets/
*.secret
